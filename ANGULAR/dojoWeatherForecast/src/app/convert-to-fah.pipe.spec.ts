import { ConvertToFahPipe } from './convert-to-fah.pipe';

describe('ConvertToFahPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToFahPipe();
    expect(pipe).toBeTruthy();
  });
});
